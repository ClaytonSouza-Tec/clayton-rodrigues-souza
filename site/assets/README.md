Coloque a imagem do avatar aqui com o nome `avatar.png`.

Use a imagem enviada como anexo na conversa (pixel-art) e salve em `site/assets/avatar.png`.

A página principal (`index.html`) já referencia `assets/avatar.png` e o CSS aplica corte circular (borda arredondada).

Se quiser, posso adicionar a imagem para você — envie o arquivo ou autorize que eu use o anexo atual.
